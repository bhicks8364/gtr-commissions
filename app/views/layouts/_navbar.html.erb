<nav class="navbar navbar-default">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "GTR Commissions", root_path, class: "navbar-brand" %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      
        <% if user_signed_in? %>
        <ul class="nav navbar-nav">
          <li><%= link_to "<span data-toggle='tooltip' data-placement='bottom' title='View Profile'>#{current_user.username}</span>".html_safe, profile_path(current_user) if user_signed_in? %></li>
          <li><%= link_to "Customers", customers_path %></li>
          <li><%= link_to "Commission Reports", commission_reports_path %></li>
          <% if current_user.admin? %>
          <li><%= link_to "Users", users_index_path  %></li>
          <%= render "layouts/dropdowns/account_manager" %>
          <%= render "layouts/dropdowns/recruiter" %>
          <%= render "layouts/dropdowns/support" %>
          <%= render "layouts/dropdowns/admin" %>
          <% end %>
        </ul>
        
        <% else %>
        <ul class="nav navbar-nav">
          <li><%= link_to "SIGN IN", new_user_session_path(redirect_to: request.path) %></li>
          <li><%= link_to "SIGN UP", new_user_registration_path %></li>
        </ul>
        <% end %>
        
      <ul class="nav navbar-nav navbar-right">
        <li><%= link_to "<i class='fa fa-power-off fa-lg'></i>".html_safe, destroy_user_session_path, method: :delete if user_signed_in? %></li>
      </ul>
      
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>